box: debian
build:
  steps:
    - arjen/hugo-build:
        flags: --buildDrafts=true

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - sf-zhou/gh-pages@0.2.6:
            token: $GIT_TOKEN
            domain: anguoi.github.io
            branch: master
            basedir: public