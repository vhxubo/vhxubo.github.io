box: debian
build:
  steps:
    - arjen/hugo-build:
        flags: --buildDrafts=false

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - nztomas/gh-pages@0.2.4:
        token: $GIT_TOKEN
        domain: blog.zxbzn.com
        branch: master
        basedir: public